{
    "variables": {
      "aws_access_key": "",
      "aws_secret_key": ""
    },
    "builders": [
      {
        "type": "amazon-ebs",
        "access_key": "{{user `aws_access_key`}}",
        "secret_key": "{{user `aws_secret_key`}}",
        "region": "us-west-2",
        "source_ami": ${{ github.event.inputs.amiId }},
        "instance_type": "t2.micro",
        "ssh_username": "ubuntu",
        "ami_name": "packer-builder-goldenami-{{timestamp}}"
      }
    ],
    "provisioners": [
      {
        "type": "shell",
        "script": "provision-script.sh"
      }
    ]
  }
